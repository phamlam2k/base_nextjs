import { useForm } from "react-hook-form";

const AccountForm = () => {
  const methods = useForm({});

  return (
    <div>
      <h1>Account Form</h1>
    </div>
  );
};

export default AccountForm;
